{
  "name": "@avala-ai/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for the Avala API",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "avala-mcp-server": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "lint": "tsc --project ../sdk/tsconfig.json --emitDeclarationOnly && tsc --noEmit"
  },
  "dependencies": {
    "@avala-ai/sdk": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.25.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
