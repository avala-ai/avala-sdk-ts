# MCP tool inventory

> The **Serialization** column describes each tool's OWN handling only.
> Since `src/egress.ts`, every ordinary tool result is scrubbed at a single
> server-level boundary regardless of this column — a tool marked **raw** is no
> longer unprotected, it simply does no redaction of its own. The resolver's
> exact two-field result is the sole deliberate capability release.

Generated by `bun scripts/tool-inventory.ts`. 80 tools (57 read, 23 mutation).

| Tool | Kind | Upstream route | Scope | Annotated | outputSchema | Serialization | Inputs |
|---|---|---|---|---|---|---|---|
| `list_datasets` | read | `GET dataset-list` | datasets.read | yes | yes | catalog | dataType, name, status, visibility, limit, cursor, detail |
| `get_dataset` | read | `GET dataset-detail` | datasets.read | yes | yes | catalog | uid, detail |
| `list_sequences` | read | `GET dataset-sequence-item-list-by-owner-and-dataset-name` | datasets.read | yes | yes | catalog | owner, slug, limit, cursor, detail |
| `get_sequence` | read | `GET dataset-sequence-item-detail-by-owner-and-dataset-name` | datasets.read | yes | yes | catalog | owner, slug, sequenceUid, detail |
| `preview_curation_candidates` | read | `GET dataset-curation-preview` | datasets.read | yes | yes | catalog | datasetUid, unit, qcDimension, requiredState, minimumConsensus, projectUid, taskName, deliverableId, deliverableStateField, excludeSliceUid, limit, cursor |
| `get_frame` | read | _hand-written_ | datasets.read | **no** | **no** | **raw** | owner, slug, sequenceUid, frameIdx, include_attribution, detail |
| `get_calibration` | read | _hand-written_ | datasets.read | **no** | **no** | **raw** | owner, slug, sequenceUid, detail |
| `get_dataset_health` | read | `GET dataset-health-by-owner-and-name` | datasets.read | yes | yes | catalog | owner, slug, detail |
| `get_dataset_readiness` | read | `GET dataset-health-by-owner-and-name` | datasets.read | yes | yes | catalog | owner, slug, requiredCalibrations, detail |
| `list_capture_submissions` | read | `GET dataset-capture-submissions` | datasets.read | yes | yes | catalog | datasetUid, status, limit, cursor, detail |
| `get_capture_submission` | read | `GET capture-submission-detail` | datasets.read | yes | yes | catalog | resultUid, include_attribution, detail |
| `list_capture_campaigns` | read | `GET dataset-capture-campaigns` | datasets.read | yes | yes | catalog | datasetUid, detail |
| `create_dataset` | mutation | _hand-written_ | — | **no** | **no** | **raw** | name, slug, dataType, visibility, createMetadata, providerConfig, ownerName |
| `list_projects` | read | _hand-written_ | projects.read | **no** | **no** | **raw** | limit, cursor, detail |
| `get_project` | read | _hand-written_ | projects.read | **no** | **no** | **raw** | uid, detail |
| `get_workspace_stats` | read | _hand-written_ | datasets.read & projects.read & exports.read | **no** | **no** | **raw** | detail |
| `list_tasks` | read | `GET customer-task-list` | tasks.read | yes | yes | catalog | project, status, limit, cursor, detail |
| `get_task` | read | `GET customer-task-detail` | tasks.read | yes | yes | catalog | uid, detail |
| `list_agents` | read | `GET agent-list` | agents.read | yes | yes | catalog | limit, cursor, detail |
| `get_agent` | read | `GET agent-detail` | agents.read | yes | yes | catalog | uid, detail |
| `create_agent` | mutation | _hand-written_ | — | **no** | **no** | **raw** | name, events, callbackUrl, description, project, taskTypes |
| `delete_agent` | mutation | _hand-written_ | — | **no** | **no** | **raw** | uid |
| `list_annotation_issues_by_sequence` | read | `GET sequence-annotation-issues` | qc.read | yes | yes | catalog | sequenceUid, datasetItemUid, projectUid, detail |
| `create_annotation_issue` | mutation | _hand-written_ | — | **no** | **no** | **raw** | sequenceUid, toolUid, problemUid, datasetItemUid, projectUid, priority, severity, description, wrongClass, correctClass, shouldReAnnotate, shouldDelete, framesAffected, coordinates, queryParams, objectUid |
| `update_annotation_issue` | mutation | _hand-written_ | — | **no** | **no** | **raw** | sequenceUid, issueUid, status, priority, severity, description, toolUid, problemUid, wrongClass, framesAffected |
| `delete_annotation_issue` | mutation | _hand-written_ | — | **no** | **no** | **raw** | sequenceUid, issueUid |
| `list_annotation_issues_by_dataset` | read | `GET dataset-issues` | qc.read | yes | yes | catalog | owner, datasetSlug, sequenceUid, detail |
| `get_annotation_issue_metrics` | read | `GET dataset-issue-metrics` | qc.read | yes | yes | catalog | owner, datasetSlug, sequenceUid, detail |
| `list_qc_tools` | read | `GET qc-available-tools` | qc.read | yes | yes | catalog | datasetType, detail |
| `list_webhooks` | read | `GET webhook-list` | webhooks.read | yes | yes | catalog | limit, cursor, detail |
| `create_webhook` | mutation | _hand-written_ | — | **no** | **no** | **raw** | targetUrl, events |
| `delete_webhook` | mutation | _hand-written_ | — | **no** | **no** | **raw** | uid |
| `list_storage_configs` | read | `GET storage-config-list` | storage.read | yes | yes | catalog | limit, cursor, detail |
| `create_storage_config` | mutation | _hand-written_ | — | **no** | **no** | **raw** | name, provider, s3BucketName, s3BucketRegion, s3BucketPrefix, s3IsAccelerated, gcStorageBucketName, gcStoragePrefix |
| `test_storage_config` | mutation | _hand-written_ | — | **no** | **no** | **raw** | uid |
| `delete_storage_config` | mutation | _hand-written_ | — | **no** | **no** | **raw** | uid |
| `create_export` | mutation | _hand-written_ | — | **no** | **no** | **raw** | project, dataset |
| `list_exports` | read | `GET export-list` | exports.read | yes | yes | catalog | limit, cursor, detail |
| `get_export_status` | read | `GET export-detail` | exports.read | yes | yes | catalog | uid, detail |
| `list_quality_targets` | read | `GET quality-targets-list` | qc.read | yes | yes | catalog | projectUid, limit, cursor, detail |
| `get_result_acceptance` | read | `GET result-acceptance` | datasets.read | yes | yes | catalog | resultUid, detail |
| `get_campaign_acceptance_summary` | read | `GET campaign-acceptance-summary` | datasets.read | yes | yes | catalog | projectUid, detail |
| `get_campaign_acceptance_coverage` | read | `GET campaign-acceptance-coverage` | datasets.read | yes | yes | catalog | projectUid, axes, detail |
| `evaluate_quality` | mutation | _hand-written_ | — | **no** | **no** | **raw** | projectUid |
| `get_consensus_summary` | read | `GET consensus-summary` | qc.read | yes | yes | catalog | projectUid, detail |
| `compute_consensus` | mutation | _hand-written_ | — | **no** | **no** | **raw** | projectUid |
| `fleet_list_devices` | read | `GET fleet-device-list` | fleet.read | yes | yes | catalog | status, type, limit, cursor, detail |
| `fleet_get_device` | read | `GET fleet-device-detail` | fleet.read | yes | yes | catalog | uid, detail |
| `fleet_list_recordings` | read | `GET fleet-recording-list` | fleet.read | yes | yes | catalog | device, status, limit, cursor, detail |
| `fleet_get_recording` | read | `GET fleet-recording-detail` | fleet.read | yes | yes | catalog | uid, detail |
| `fleet_list_events` | read | `GET fleet-event-list` | fleet.read | yes | yes | catalog | recording, device, type, severity, limit, cursor, detail |
| `fleet_list_alerts` | read | `GET fleet-alert-list` | fleet.read | yes | yes | catalog | status, severity, device, rule, limit, cursor, detail |
| `fleet_list_rules` | read | `GET fleet-rule-list` | fleet.read | yes | yes | catalog | enabled, limit, cursor, detail |
| `fleet_register_device` | mutation | _hand-written_ | — | **no** | **no** | **raw** | name, type, firmwareVersion, tags |
| `fleet_acknowledge_alert` | mutation | _hand-written_ | — | **no** | **no** | safeStringify | uid |
| `list_organizations` | read | `GET organization-list-create` | organizations.read | yes | yes | catalog | limit, cursor, detail |
| `get_organization` | read | `GET organization-detail` | organizations.read | yes | yes | catalog | slug, detail |
| `list_slices` | read | `GET slices-by-owner-list` | slices.read | yes | yes | catalog | owner, limit, cursor, detail |
| `get_slice` | read | `GET slice-by-owner-and-name` | slices.read | yes | yes | catalog | owner, slug, detail |
| `resolve_asset_handle` | read | _hand-written_ | any(datasets.read, organizations.read, slices.read, exports.read) | yes | yes | **raw** | handle |
| `create_annotation_pipeline` | mutation | _hand-written_ | — | **no** | **no** | **raw** | name, slug, dataType, projectUid |
| `get_fleet_health` | read | `GET fleet-device-list, GET fleet-alert-list, GET fleet-recording-list` | fleet.read | yes | yes | catalog | deviceType, detail |
| `get_project_quality_summary` | read | _hand-written_ | projects.read & qc.read | **no** | **no** | **raw** | projectUid |
| `get_workspace_overview` | read | _hand-written_ | organizations.read & datasets.read & projects.read & exports.read | **no** | **no** | **raw** | — |
| `get_workforce_operations_overview` | read | `GET workforce-operations-overview` | workforce.read | yes | yes | catalog | windowDays, attentionLimit |
| `list_workforce_batches` | read | `GET workforce-batches` | workforce.read | yes | yes | catalog | organizationUid, projectUid, datasetUid, sequenceUid, status, priority, limit, cursor |
| `list_workforce_groups` | read | `GET workforce-groups` | workforce.write | yes | yes | catalog | search, hasActiveApprovedCoworkers, limit, cursor |
| `get_workforce_batch_attention` | read | `GET workforce-batch-attention` | workforce.read | yes | yes | catalog | batchUid |
| `list_workforce_batch_units` | read | `GET workforce-batch-units` | workforce.read | yes | yes | catalog | batchUid, status, assigned, workflowRole, limit, cursor |
| `get_workforce_sequence_status` | read | `GET workforce-sequence-status` | workforce.read | yes | yes | catalog | sequenceUid |
| `list_workforce_assignment_candidates` | read | `GET workforce-work-unit-assignment-candidates` | workforce.write | yes | yes | catalog | workUnitUid, windowDays, limit, cursor |
| `create_workforce_batch` | mutation | `POST workforce-batch-create` | workforce.write | yes | yes | catalog | name, projectUid, sequenceUid, expectedSequenceStatus, expectedSequenceUpdatedAt, expectedWorkflowRevisionUid, workUnits, reason |
| `set_workforce_batch_priority` | mutation | `POST workforce-batch-priority` | workforce.write | yes | yes | catalog | batchUid, expectedPriority, priority, reason |
| `set_workforce_batch_status` | mutation | `POST workforce-batch-status` | workforce.write | yes | yes | catalog | batchUid, expectedStatus, status, reason |
| `set_workforce_sequence_status` | mutation | `POST workforce-sequence-status-transition` | workforce.write | yes | yes | catalog | sequenceUid, expectedStatus, expectedUpdatedAt, expectedWorkflowRevisionUid, status, reason |
| `assign_workforce_work_unit` | mutation | `POST workforce-work-unit-assign` | workforce.write | yes | yes | catalog | workUnitUid, coworkerUid, expectedBatchUid, expectedBatchStatus, expectedLineContext, expectedStatus, expectedAssigned, expectedUpdatedAt, reason |
| `deassign_workforce_work_unit` | mutation | `POST workforce-work-unit-deassign` | workforce.write | yes | yes | catalog | workUnitUid, expectedBatchUid, expectedLineContext, expectedStatus, expectedUpdatedAt, reason |
| `staff_query` | read | _hand-written_ | mcp.query | yes | **no** | other | sql, params, limit |
| `staff_aggregate` | read | _hand-written_ | mcp.query | yes | **no** | other | table_name, aggregation, column, group_by, filters |
| `staff_describe_table` | read | _hand-written_ | mcp.query | yes | **no** | other | table_name |

## Descriptions

- `list_datasets` — List datasets in your workspace. Default detail is concise (uid, name, slug, dataType, unit-bearing counts, status, owner, updatedAt). Labels, nested projects, and opaque handles for credential-bearing media require detail=full. Supports server-side filters: data type, name, status, visibility.
- `get_dataset` — Get a dataset. Default detail is concise (identity, unit-bearing counts, status, owner, updatedAt). Use detail=full for labels, nested projects, and media.
- `list_sequences` — List sequences for a dataset (paginated). Each sequence includes uid, key, status, and frame count. Opaque featured-image handles require detail=full.
- `get_sequence` — Get a dataset sequence. Default detail is identity and status. Use detail=full for the frames array (LiDAR JSON metadata for every frame), opaque media handles, and predefined labels. Export-snippet attribution is never returned in the frame list; use get_frame with detail=full and include_attribution=true for one frame when identity is required.
- `preview_curation_candidates` — Preview a bounded, read-only set of Physical AI training candidates under one explicit workflow or QC criterion. Returns disjoint selected, excluded-membership, missing-evidence, and rejected counts plus opaque pagination. It never creates a Slice, recomputes QC, or changes workflow state. datasets.read is always required; the provider conditionally enforces projects.read, tasks.read, qc.read, and slices.read for criteria that use those resources. Consensus currently fails closed as insufficient evidence until scores carry immutable run provenance.
- `get_frame` — Get a single frame's LiDAR JSON metadata (camera model, intrinsics, device pose, per-camera rig). Default detail is frameIndex, model, and key. Use detail=full for the complete rig payload and opaque media handles; within that payload, annotator and reviewer identity nested in export snippets is omitted unless include_attribution=true because attribution is personal data. Intended for post-ingest validation — diff what you uploaded against what the server sees.
- `get_calibration` — Get a sequence's canonicalized per-camera rig (position, heading, intrinsics, projection model) derived from frame[0]. Default detail is sequenceUid. Use detail=full for the camera array.
- `get_dataset_health` — Get a read-only ingest/health snapshot for a dataset: live frame totals for sequence data or asset counts for non-sequence media, sequence count, ingest_ok flag, and any issues detected. Default detail omits the per-sequence array. Useful for validating a dataset after upload without opening Mission Control. For the reconstruction question ('is this ready to rebuild?') use get_dataset_readiness — ingestOk does not see missing calibration.
- `get_dataset_readiness` — Check whether a dataset can enter a selected photoreal-reconstruction recipe. The caller must declare that recipe's required calibration artifacts because the health endpoint cannot distinguish an absent sensor from an uncalibrated one. Required stored calibration also requires sequence-shaped input; recipes with no stored calibration requirement may use non-sequence media assets. Required missing artifacts are blocking; non-required calibration checks are skipped. Results use named pass/fail/insufficient_evidence/skipped checks — never a ready boolean or opaque score. Reads the customer-reachable DatasetHealthView route. Default detail keeps counts; missing sequence UIDs and ingest issue strings require detail=full.
- `list_capture_submissions` — List a dataset's Physical AI capture submissions. Default detail is result identity and review status. Use detail=full for opaque playback/thumbnail handles, media metadata, machine acceptance, and campaign task context. Submitter and reviewer identity are never returned by this list tool.
- `get_capture_submission` — Get one Physical AI capture submission by result ID. Default detail is identity and review status. Use detail=full for opaque playback/thumbnail handles, media metadata, reviewer decision, machine acceptance, and campaign task context. Submitter and reviewer identity require include_attribution=true because attribution is personal data.
- `list_capture_campaigns` — List every Physical AI capture campaign feeding a dataset. Default detail keeps campaign identity and progress roll-up. Use detail=full for capture config and per-task-description instructions. Upstream returns the full campaign set in one payload — it has no cursor; we do not emulate pagination client-side.
- `create_dataset` — Create a new dataset for annotation. Supports image, video, lidar, and mcap data types.
- `list_projects` — List the annotation projects this credential can see, with status and progress. Reads the caller's own project scope via /users/me/projects/; it does NOT list every project on the instance, which is a staff-only surface. Default detail is concise (uid, name, status, owner, updatedAt). Use detail=full for configuration blobs. Pagination uses the upstream cursor when the SDK page provides one.
- `get_project` — Get one of the caller's own projects. Default detail is identity and status. Use detail=full for configuration and current progress. Staff listing of every project on the instance is a different surface.
- `get_workspace_stats` — Get a summary of workspace usage including dataset count and project count. Project count is the caller's own scope via /users/me/projects/, not every project on the instance. Already a small payload; detail is accepted for consistency with other get tools.
- `list_tasks` — List tasks with optional server-side filtering by project or status. Default detail is identity and status.
- `get_task` — Get a task. Default detail is identity and status. Use detail=full for the rest of the task record.
- `list_agents` — List automation agents. Default detail is identity and status. Callback URLs and execution stats require detail=full.
- `get_agent` — Get an automation agent. Default detail is identity and status. Use detail=full for callback URL, events, and execution stats.
- `create_agent` — Create a new automation agent with event subscriptions and a callback URL.
- `delete_agent` — Delete an automation agent by its UID.
- `list_annotation_issues_by_sequence` — List annotation issues for a specific sequence. Default detail is identity and status. Coordinates and class-correction fields require detail=full. Reporter identity is never returned by this list tool. Upstream is an unpaginated list — no cursor is invented client-side.
- `create_annotation_issue` — Create a new annotation issue on a sequence.
- `update_annotation_issue` — Update an existing annotation issue.
- `delete_annotation_issue` — Delete an annotation issue.
- `list_annotation_issues_by_dataset` — List annotation issues for a specific dataset. Default detail is identity and status. Reporter identity is never returned by this list tool. Upstream is an unpaginated list — no cursor is invented client-side.
- `get_annotation_issue_metrics` — Get annotation issue metrics for a dataset. Default detail is the status/priority/severity counts.
- `list_qc_tools` — List available QC annotation tools for a dataset type. Default detail is uid, name, and datasetType. Problem catalogs require detail=full. Upstream is an unpaginated list.
- `list_webhooks` — List webhook subscriptions. Default detail is identity and status. Target URLs require detail=full.
- `create_webhook` — Create a new webhook subscription for specific events.
- `delete_webhook` — Delete a webhook subscription by its UID.
- `list_storage_configs` — List storage configurations. Default detail is identity, provider, and verification status. Bucket names and prefixes require detail=full.
- `create_storage_config` — Create a new storage configuration (S3 or Google Cloud Storage) with non-sensitive settings only (bucket, region, prefix). Credentials must be provisioned separately through the Avala web console — this tool will NOT accept access keys or service-account JSON, because any value passed here is stored in the LLM's conversation context and logs.
- `test_storage_config` — Test connectivity for a storage configuration.
- `delete_storage_config` — Delete a storage configuration by its UID.
- `create_export` — Trigger a new export for a dataset or project.
- `list_exports` — List exports. Default detail is identity, format, status, and counts. Opaque download-asset handles require detail=full; resolve one explicitly only when the artifact is needed.
- `get_export_status` — Check whether an export is still processing, completed, or failed. A completed export's opaque download-asset handle requires detail=full; resolve it explicitly only when the artifact is needed.
- `list_quality_targets` — List quality targets for a project. Default detail is identity, metric, and breach state. Notification lists require detail=full.
- `get_result_acceptance` — Get the machine acceptance verdict for one capture submission. Default detail is resultUid and verdict. Criteria and signals require detail=full.
- `get_campaign_acceptance_summary` — Get a capture campaign's acceptance rates. Default detail is headline rates. Breakdowns require detail=full.
- `get_campaign_acceptance_coverage` — Get under-covered values across reviewer-accepted captures. Default detail is totalAccepted. Axis cells require detail=full.
- `evaluate_quality` — Evaluate all quality targets for a project and return their current status.
- `get_consensus_summary` — Get a consensus summary for a project. Default detail is mean/median scores and item counts. Distribution histograms require detail=full.
- `compute_consensus` — Trigger consensus computation for a project.
- `fleet_list_devices` — List fleet devices with optional filters.
- `fleet_get_device` — Get detailed information about a specific fleet device.
- `fleet_list_recordings` — List fleet recordings with optional filters.
- `fleet_get_recording` — Get detailed information about a specific recording.
- `fleet_list_events` — List fleet events with optional filters.
- `fleet_list_alerts` — List fleet alerts with optional filters.
- `fleet_list_rules` — List fleet rules with optional filters.
- `fleet_register_device` — Register a new fleet device.
- `fleet_acknowledge_alert` — Acknowledge a fleet alert.
- `list_organizations` — List organizations you are a member of. Default detail is identity, plan, and membership. Opaque logo-asset handles and contact fields require detail=full.
- `get_organization` — Get an organization. Default detail is identity and counts. Use detail=full for contact fields, domains, and an opaque logo-asset handle.
- `list_slices` — List slices for an owner (user or organization). Default detail is identity, status, and asset count. Nested sub-slices and opaque featured-media handles require detail=full.
- `get_slice` — Get a slice. Default detail is identity, status, and asset count. Use detail=full for nested sub-slices, source data, and opaque featured-media handles.
- `resolve_asset_handle` — Exchange one opaque asset handle for a fresh asset URL after server-enforced MCP elicitation confirms release. Unsupported, declined, forged, or cross-handle replayed confirmation fails closed. The original REST resource is fetched again with the current credential, so revoked access fails at resolve time; expiresAt reports provider-declared expiry when available.
- `create_annotation_pipeline` — Create a dataset and optionally trigger an export for a project. The dataset is always created first. If the export step fails, the response includes the dataset that was created and the export error so nothing is silently lost.
- `get_fleet_health` — Get a fleet health overview — device counts by status, open alerts by severity, and recent recording count. Counts are from the first page of results (up to 100 devices, 100 alerts). Use when a user asks about fleet status or device health.
- `get_project_quality_summary` — Get a quality picture for a project — project details, quality target breach status, and consensus scores. Use when a user asks 'how is quality on project X?' or wants to check quality thresholds.
- `get_workspace_overview` — Get a high-level overview of the workspace — organizations, recent datasets, recent projects, and recent exports. Use when a user first connects or asks 'what do I have?' or 'show me my workspace.'
- `get_workforce_operations_overview` — Staff only: get an aggregate, PII-free snapshot of coworker readiness, sessions, and live work-queue attention signals for the Physical AI data loop.
- `list_workforce_batches` — Staff only: enumerate a bounded page of Physical AI labeling production lines with exact organization, project, dataset, sequence, lifecycle, and priority filters. Returns opaque context IDs and raw unit-status counts, never customer or batch names, coworker identity, task payloads, URLs, groups, configuration, comments, or pay data.
- `list_workforce_groups` — Staff only: discover stable coworker-group UIDs and internal operational labels for reviewed Physical AI batch planning. Requires workforce.write and returns aggregate membership readiness only, never member identities, permission topology, pay, performance, or live capacity.
- `get_workforce_batch_attention` — Staff only: drill into one batch from the workforce operations overview using fixed, aggregate role-by-status and queue-age signals. Supports Physical AI labeling and review operations without exposing coworker or work-unit rows.
- `list_workforce_batch_units` — Staff only: inspect a bounded page of opaque work-unit state for one Physical AI labeling batch before planning a unit-level operation. Returns whether a unit is assigned, but never coworker identity, task payloads, URLs, groups, or pay data.
- `get_workforce_sequence_status` — Staff only: inspect one Physical AI sequence's exact workflow status, update timestamp, workflow revision, and only the currently authorized next transitions before planning a status change. Supports legacy and custom workflows without exposing sequence contents or workflow definitions; deliverable workflows return transitionMode=deliverable and no legacy sequence edges.
- `list_workforce_assignment_candidates` — Staff only: list a bounded page of opaque coworker IDs currently eligible for one unassigned Physical AI labeling unit, with raw recent completion, abandonment, and error counts scoped to its exact organization, task, and workflow role. Requires workforce.write; returns no ranking or composite score and excludes names, contact details, profiles, group names, pay data, and current work details.
- `create_workforce_batch` — Staff only: create one unavailable, sequence-scoped Physical AI labeling production line with 1–100 backlog work units after explicit human approval. Requires the exact sequence status, update timestamp, and workflow revision returned by get_workforce_sequence_status; the server derives all coworker routes and refuses arbitrary URLs or configuration.
- `set_workforce_batch_priority` — Staff only: change one Physical AI labeling work batch between medium and high priority after an explicit human approval. Requires the exact priority observed by get_workforce_batch_attention and refuses stale changes.
- `set_workforce_batch_status` — Staff only: move one Physical AI labeling production line between available, unavailable, and archived after explicit human approval. Requires the exact lifecycle status observed by list_workforce_batches and refuses stale changes.
- `set_workforce_sequence_status` — Staff only: apply one workflow-authorized status transition to a Physical AI sequence after explicit human approval. Requires the exact status, update timestamp, and workflow revision observed by get_workforce_sequence_status; custom status IDs are supported and stale or unavailable edges are refused.
- `assign_workforce_work_unit` — Staff only: assign one eligible coworker to one backlog Physical AI labeling unit after explicit human approval. Requires the exact batch status, line context, unit state, timestamp, and opaque coworker ID returned by the inspection tools.
- `deassign_workforce_work_unit` — Staff only: clear the current coworker assignment from one in-progress Physical AI labeling unit and return it to backlog after explicit human approval. Requires the exact batch, line context, status, and update timestamp observed by list_workforce_batch_units.
- `staff_query` — Staff only: run a read-only SQL SELECT in the Avala staff sandbox (cross-tenant; validated against an allow-list of tables and columns). Use %s placeholders with params for values.
- `staff_aggregate` — Staff only: aggregate (count/sum/avg/min/max) over a table in the Avala staff sandbox, optionally grouped and filtered.
- `staff_describe_table` — Staff only: describe an allow-listed table's columns, types, and indexes in the Avala staff sandbox.
